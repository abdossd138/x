import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.football.auction.game',
  appName: 'FootballAuctionGame',
  webDir: 'dist'
};

export default config;
